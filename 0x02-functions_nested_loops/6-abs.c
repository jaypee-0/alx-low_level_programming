#include "main.h"

/**
 * _abs - computes absolute value
 * @n: value to be computed for absolute value
 *
 * Description: Computes the absolute value for an integer
 *
 * Return: the absolute value
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);

	return (n);
}
